# Changelog

## 1.4.1 - 2026-08-16

- 修复程序启动后缓存 Windows 系统代理的问题。
- 运行期间开启或关闭 Clash/VPN 后，网络请求会重新读取当前代理设置，无需重启软件。
- 余额和平台用量请求的每次重试也会同步刷新代理配置。

## 1.4.0 - 2026-08-16

- 主程序图标改为黑底、蓝色连体艺术字“DS”，与悬浮窗标题配色统一。
- Windows ICO 内置多种尺寸，改善桌面、文件管理器和任务切换界面的显示效果。
- 托盘图标直接复用同一张图标母版，并随单文件 EXE 一起打包。

## 1.3.4 - 2026-08-16

- 窗口右键菜单精简为“立即刷新”“设置账号信息”“时间维度”“打开配置目录”四项。
- 移除右键菜单中的凭证帮助、置顶、固定、最小化和退出等项目；托盘菜单保持不变。
- 右键主菜单及时间维度子菜单统一使用灰蓝底色、白字和稍亮的悬停色。

## 1.3.3 - 2026-08-16

- 修复删除按钮偶尔删除错误账户、并可能用旧输入覆盖相邻账户的问题。
- 删除操作现在始终以账户列表当前高亮项为准。
- 账户列表可见高度由 4 行增加至 8 行。
- 在添加、删除按钮下方新增“上移”和“下移”，支持调整账户顺序并保持移动项选中。

## 1.3.2 - 2026-08-16

- 修复设置窗口添加账户后错误跳回列表第一项的问题。
- 新账户添加后自动选中并立即进入编辑状态。
- 保存账号信息后立即同步左侧账户列表中的账号名和 Key 名，同时保持当前选择。

## 1.3.1 - 2026-08-16

- 账号/Key 和模型两个下拉框统一改为灰蓝色背景与白色文字。
- 常态、聚焦态、箭头区域和展开列表使用一致的高对比度配色。

## 1.3.0 - 2026-08-16

- 在账号/Key 下拉菜单下新增模型下拉菜单，启动时默认“所有模型 / All Models”。
- 模型名直接从平台用量响应动态发现，不预置 DeepSeek 模型名称。
- 支持在账号总量和可验证的单 Key 视图中按模型筛选消费、API 请求次数和 Tokens。
- 兼容 `model` 字符串、模型对象和常见模型字段名称。
- 经典按月接口回退同样保留按模型的用量分组。
- 增加动态模型发现、经典响应解析和界面筛选测试。

## 1.2.0 - 2026-08-16

- 托盘菜单与窗口右键菜单新增“时间维度 / Time Range”子菜单。
- 支持今日、昨日、近 7 天、近 30 天、本月和上月六种统计范围，启动时默认今日。
- 消费、API 请求次数和 Tokens 会随时间维度同步查询、汇总和更新标签。
- 经典按月接口回退支持跨月查询与日期过滤，覆盖近 7/30 天跨月场景。
- 切换范围时丢弃仍在途的旧范围响应，防止异步刷新造成数据串位。
- 增加自然月边界、跨月汇总、时间戳区间和中英文标签测试。

## 1.1.2 - 2026-08-16

- 托盘菜单新增可勾选的“开机自启 / Start with Windows”，默认不启用。
- 开机自启使用当前用户的 Windows `HKCU\...\Run` 启动项，无需管理员权限。
- 修复“固定右下角”启用后按住标题仍会自动取消固定并拖动窗口的问题。
- 固定状态下完全忽略拖动，取消固定后恢复移动。

## 1.1.1 - 2026-08-16

- 托盘菜单新增固定名称 `language`，支持 `中文` 与 `English`。
- 语言选择写入配置，切换后即时更新主窗口、托盘、右键菜单、设置和状态提示。
- 增加语言文案与配置持久化测试。

## 1.1.0 - 2026-08-16

- 使用 Windows DPAPI 加密 API Key 与 userToken，并迁移配置至 AppData。
- 修复多 Key 重复统计、混合口径误标、Key 匹配失败假零和跨日旧数据问题。
- 同一 userToken 的平台用量请求合并为每轮一次。
- 修复后台快照没有更新导致“保留上次数据”失效的问题。
- 修复所选时区跨月时经典接口查询错误月份的问题。
- 增加凭证掩码、关闭按钮、右键菜单、位置固定开关和日期/旧数据提示。
- 增加原子配置写入、自动化测试、构建脚本及 Windows 版本信息。
